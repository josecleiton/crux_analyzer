# Ship the compiled extension, the localization files and the built web
# bundle — nothing else. (media/web is produced by `just ext-build`.)
**
!out/**
!media/web/**
!l10n/**
!package.nls*.json
!LICENSE*
!README*
# The notices of the bundled web code. Also present under media/web, but at the
# package root it is where a reader — and the marketplace listing — looks.
!THIRD-PARTY-NOTICES.md
