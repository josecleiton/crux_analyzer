# crux_analyzer documentation

> 🌐 **English** · [Português (Brasil)](pt-BR/README.md)

crux_analyzer is a **semantic analyzer** for Rust + [Crux](https://redbadger.github.io/crux/)
applications: it statically extracts each Core's state machines — states,
transitions, triggering events and requested effects — into an intermediate
model, and turns that model into living documentation (interactive web UI,
Mermaid/Markdown docs).

| Document | What it covers |
| --- | --- |
| [Architecture](architecture.md) | Monorepo layout, the model-first design, hard rules, data flow |
| [Parser](parser.md) | How extraction works: detection, guards, value-flow, composites, warnings |
| [Schema](schema.md) | The JSON contract every client consumes |
| [CLI](cli.md) | `crux-analyzer generate | docs`, `--watch`, formats |
| [Web UI](web-ui.md) | Sections, inspector, filtering, simulation, layout engine |
| [VS Code extension](vscode.md) | The state machines beside the code, regenerating on save |
| [Internationalization](i18n.md) | Locales, catalogs, what must never be translated |
| [Security](security.md) | Threat model, the development rules, what is deliberately guaranteed |
| [Development](development.md) | Setup, tests, corpus, conventions, validation pipeline |
| [Roadmap](roadmap.md) | Planned work, in order, and what is deliberately not being done |
| [Example output](examples/mini-recorder.md) | Docs generated by the CLI from the test fixture |

This English set is the **source**; `docs/pt-BR/` mirrors it. See
[i18n.md](i18n.md) for what gets translated and what must not.

## The elevator pitch

Crux apps are state machines by construction: a `Model`, an `Event` enum, and
an `update` function that moves the model between states and requests
`Effect`s. That structure is documentation — but it lives scattered across
`match` arms, guards and helper functions, and it drifts the moment nobody is
looking.

crux_analyzer reads the source (via the `syn` AST — it never depends on Crux
itself), reconstructs the machines, and emits a small JSON model. Everything
else — the web UI, the doc generators, the VS Code extension, future clients
(PlantUML/HTML) — consumes only that model. Run it with `--watch` and the
documentation stays alive as the code changes.
