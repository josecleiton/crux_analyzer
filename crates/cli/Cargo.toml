[package]
name = "crux-analyzer-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "CLI for crux_analyzer: analyzes a Rust + Crux crate and emits the semantic model as JSON."

[[bin]]
name = "crux-analyzer"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
crux-analyzer-docgen = { path = "../docgen", version = "0.1.0" }
crux-analyzer-i18n = { path = "../i18n", version = "0.1.0" }
crux-analyzer-model = { path = "../model", version = "0.1.0" }
crux-analyzer-parser = { path = "../parser", version = "0.1.0" }
notify = "8"
serde_json = { workspace = true }
